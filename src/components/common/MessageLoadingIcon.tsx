import React from "react";
import styles from "./MessageLoadingIcon.module.scss";

const MessageLoadingIcon = () => {
  return <div className={styles.lds_dual_ring}></div>;
};

export default MessageLoadingIcon;
